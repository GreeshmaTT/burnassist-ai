# Copy to burn_project/.env for local use (never commit .env).
# Render: set DJANGO_SECRET_KEY in the dashboard.

DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Place burn_model_final.keras and labels.pkl in burn_project/predictor/ (gitignored).
# On Render, use a Secret File, disk, or build script before the web process starts.
